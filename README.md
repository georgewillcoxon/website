# georgewillcoxon.com

Personal website of George Willcoxon. Built with [Quarto](https://quarto.org); the rendered site in `docs/` is served by GitHub Pages.

To rebuild after editing any `.qmd` file: `quarto render`, then commit both the source change and the regenerated `docs/`.
